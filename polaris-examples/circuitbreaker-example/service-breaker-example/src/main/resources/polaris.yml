global:
  # configuration for connecting the polaris server
  serverConnector:
    # target server address
    addresses:
      - 9.134.5.52:8091
  statReporter:
    enable: true
    plugin:
      prometheus:
        port: 28081